{
  "name": "trail-weather-window-contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test",
    "check": "node --check src/forecast-contract.js"
  }
}
